package com.earnix.eo.gui.correlation;

import org.junit.jupiter.api.BeforeAll;
import org.junit.jupiter.api.Test;

/**
 * @author Taras Maslov
 * 12/5/2018
 */
class CorrelationMatrixGraphTest {

    static CorrelationMatrixGrid graph;

    @BeforeAll
    static void before() {
//        CorrelationMatrix matrix = new CorrelationMatrix(
//                Arrays.asList(CellType.NUMERIC, CellType.NUMERIC, CellType.NUMERIC, CellType.NUMERIC, CellType.NUMERIC, CellType.NUMERIC),
//                Arrays.asList("A", "AB", "ABC", "ABCD", "ABCDE", "ABCDEF"),
//                new double[][]{{0, 1,}}
//        )
//        graph = new CorrelationMatrixGraph();
    }

    @Test
    void createCell() {
//        graph.createCell(1, 1)
    }
}